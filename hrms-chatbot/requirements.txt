# ── Web framework ────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.32.0

# ── HTTP client (calls the existing Node.js HRMS API) ────────────────────────
httpx>=0.27.0

# ── Settings & validation ─────────────────────────────────────────────────────
pydantic>=2.10.0
pydantic-settings>=2.6.0

# ── JWT verification (same secret as Node.js server) ─────────────────────────
python-jose[cryptography]>=3.3.0

# ── NLP — spaCy (entity extraction) ──────────────────────────────────────────
spacy>=3.8.0
# After installing spacy run: python -m spacy download en_core_web_sm

# ── NLP — scikit-learn (intent classification) ───────────────────────────────
scikit-learn>=1.9.0
joblib>=1.6.0

# ── Session cache (optional — in-memory used if REDIS_URL is empty) ───────────
redis>=5.2.0

# ── Direct DB (for terminated employee lookup, bypasses Node.js) ─────────────
asyncpg>=0.30.0
pytest>=8.3.0
pytest-asyncio>=0.24.0
aiosqlite>=0.20.0
sqlalchemy>=2.0.0
asyncpg>=0.30.0
